<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="shortcut icon" type="image/svg+xml" href="{{ asset('assets/icons/icon.svg') }}">
        <link rel="stylesheet" type="text/css" href="{{ asset('assets/css/w3.css?v=1.0') }}">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&family=Noto+Sans&display=swap">
        <style>
        body {
    margin: 0;
    overflow: hidden;
}
.contenedor {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}
.fondo{
    height: 100%;
    width: 100%;
    background-image: url(https://oauth.genotipia.com/public/assets/img/backgroundLogin.svg);
    background-attachment: fixed;
    filter: blur(8px);
    -webkit-filter: blur(8px);
    background-position: bottom;
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    top: 0;
    z-index: -1;
}
.cuadroTexto {
    padding: 2em;
    gap: 24px;
    margin: auto 50px;
    border-radius: 16px;
    background: rgba(245, 242, 255, 0.5);
    box-shadow: rgba(17, 17, 26, 0.05) 0px 1px 0px, rgba(17, 17, 26, 0.1) 0px 0px 8px;
    backdrop-filter: blur(7.1px);
    -webkit-backdrop-filter: blur(7.1px);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
}
button {
    width: 100%;
    font-weight: 700;
    font-family: "Roboto", sans-serif;
    color: white;
    border-radius: 8px;
    font-size: 14px;
}
.cabecera {
    display: flex;
    flex-direction: row;
    align-content: center;
    align-items: center;
    gap: 25px;
    width: 100%;
    justify-content: flex-start;
}
.w3-container.w3-theme h2 {
    font-size: 12pt;
    font-weight: 600;
}

.w3-theme {
    color: black;
    background-color: white;
    border-radius: 8px;
    font-size: 12pt!important;
    height: 40px;
}
span.flecha:before {
    content: '';
    position: absolute;
    display: block;
    width: 8px;
    height: 8px;
    border-top: solid 2px #6c8fff;
    border-right: solid 2px #6c8fff;
    top: 0;
    bottom: 0;
    margin: auto;
    left: -8px;
    transform: rotate(45deg);
}

span.flecha:after {
    content: '';
    position: absolute;
    display: block;
    width: 15px;
    height: 2px;
    background: #6c8fff;
    top: 0;
    bottom: 0;
    margin: auto;
    left: -16px;
}
span.flecha {
    width: 0px;
    height: 40px;
    position: relative;
}
.botones {
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    gap: 25px;
}
form.w3-container {
    width: 100%;
    text-align: center;
}
.cuadroTexto input {
    background: white!important;
    border-radius: 4px;
    border: 1px solid #D9D9D9!important;
    outline: none;
    width: 100%;
    font-size: 14px;
    padding: 10px 15px;
}
label.w3-text-theme {
    display: block;
    text-align: left;
    font-size: 14px;
    font-weight: 400;
}
.w3-text-theme {
    color: #1F0933 !important;
}
            h1, h2, h3, h4, h5, h6 {
                font-family: 'Noto Sans', 'Segoe UI', Arial, Helvetica, sans-serif;
            }
            img {
                user-select: none;
            }
            .w3-dark-red {
                color: #fff !important;
                background-color: #d32f2f !important;
            }
            .w3-hover-dark-red:hover {
                color: #fff !important;
                background-color: #b71c1c !important;
            }
            .w3-error {
                padding: 4px;
                background: #FFEEEE;
                border: 1px solid #C00;
                color: #C00!important;
            }
            @media screen and (max-width: 600px) and (max-height: 600px) {
                .w3-display-middle {
                    position: static;
                    transform: none;
                    top: 0;
                    left: 0;
                    -ms-transform: none;
                }
            }

            a {
                text-decoration: none;
            }
            a:hover {
                text-decoration: underline;
            }
            p.w3-container.w3-center.info {
    font-size: 11pt;
    width: 240px;
    padding: 0px;
    margin-bottom: 0px;
    background: white;
    padding: 8px;
    border-radius: 5px;
}
.cabecera.w3-container.w3-center.txt {
    padding: 0px;
}
        </style>
    </head>
    <body>
        <div class="w3-content">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
