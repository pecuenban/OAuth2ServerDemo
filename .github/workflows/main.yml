name: Create Archive
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install zip-release
        run: npm install zip-release

      - name: Create zip
        run: zip -r ./build/release.zip .

      - name: Upload to FTP
        uses: anvil-solutions/Fast-FTP-Action@2.1.0
        with:
          server: 'ftp.fennech.io'
          port: 22
          username: 'test@fennech.io'
          password: '4@1E#@5h2sth'
          local_dir: './build'
